//UNIT 2 : Linked list
// There are 3 types of linked list : - Singly linked list, Doubly linked list, Circular linked list
// In singly linked list, each node contains data and a pointer to the next node
// In doubly linked list, each node contains data, a pointer to the next node and
// a pointer to the previous node
// In circular linked list, the last node points to the first node
// Function to create a new node
// #include <stdio.h>
// #include <stdlib.h>
// struct node {
//     int data;
//     struct node *next;
// };
//self-referential structure are those structures which contain a pointer to the same structure type
// type definition should be done before declaring any variable of that type
// type def struct node Node;